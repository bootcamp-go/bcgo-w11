# New Enviroment Variables
export NEW_KEY = "new_value"

# Path
export GOPATH = "$HOME/go"
export GOBIN  = "$GOPATH/bin"
# $PATH = ~/something/bin:~/something/else/bin:.../bin
# colon (:) is a separator to concatenate paths
export PATH   = $PATH:$GOBIN